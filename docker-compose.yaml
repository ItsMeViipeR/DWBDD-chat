services:
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app:Z
      - /app/node_modules
    environment:
      - VITE_API_URL=http://localhost:8080
  api:
    build: ./api
    ports:
      - "8080:8080"
    volumes:
      - ./api:/app:Z
